# Copie para .env e ajuste conforme necessário.
# NÃO comitar .env com credenciais reais.

# Backend
MONGODB_URI=mongodb://127.0.0.1:27017/kynex
# MONGODB_DB=kynex
# PORT=4100

# --- LLM (OpenRouter) ---
# Para o chat usar o LLM, defina a sua chave e deixe o modo como "full".
# Se não definir a chave, o backend devolve um fallback a indicar que está desativado.
# IMPORTANTE: nunca comite uma chave real aqui.
OPENROUTER_API_KEY=

# Modo do LLM: full | off
LLM_MODE=full

# Modelo (opcional). Default: arcee-ai/trinity-mini:free
# Pode usar KYNEX_LLM_MODEL ou OPENROUTER_MODEL.
OPENROUTER_MODEL=arcee-ai/trinity-mini:free

# Timeout do pedido ao OpenRouter (ms). Default: 12000
# OPENROUTER_TIMEOUT_MS=12000

# Compat (legado): se quiser, pode forçar enable/disable assim.
# KYNEX_LLM_ENABLED=true
# KYNEX_LLM_MODEL=arcee-ai/trinity-mini:free

# (Opcional) Simulador/IA
# KYNEX_SIM_TICK_MS=10000
# KYNEX_AI_TRAIN_DAYS=14
