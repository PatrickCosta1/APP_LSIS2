# Copie este ficheiro para .env e ajuste conforme necessário.
# NÃO comitar .env com credenciais reais.

# Backend (MongoDB)
MONGODB_URI=mongodb://127.0.0.1:27017/kynex
MONGODB_DB=kynex
# PORT=4100

# (Opcional) Telemetria sintética em dev
KYNEX_SIM_TICK_MS=10000

# LLM (OpenRouter) — opcional
# - LLM_MODE=off: desliga qualquer chamada ao LLM (default)
# - LLM_MODE=rewrite: usa LLM apenas para reescrever texto (chat + insights)
LLM_MODE=full
OPENROUTER_API_KEY=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
OPENROUTER_MODEL=tngtech/deepseek-r1t2-chimera:free
OPENROUTER_TIMEOUT_MS=7000

# Frontend (produção ou dev fora das tasks do VS Code)
VITE_API_BASE=http://localhost:4100
